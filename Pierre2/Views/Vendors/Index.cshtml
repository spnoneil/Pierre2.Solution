@using Bakery.Models;

<h1>Who's buyin'?!</h1>

@if (@Model.Count == 0)
{
  <p>You ain't got nobody buyin', yet!</p>
}

@foreach (Vendor vendor in Model)
{
  <a href="/vendors/@vendor.Id">@vendor.VendorName</a>
}

<a href="/">Go git you some more for buyin'!</a>